# App settings
APP_NAME=RAG-FastAPI
APP_HOST=0.0.0.0
APP_PORT=8000
DEBUG=True
# Security
JWT_SECRET_KEY=supersecretkey
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7
# CORS (option 1: JSON list)
ALLOWED_ORIGINS_RAW=["http://localhost", "https://example.com"]
# CORS (option 2: comma-separated — comment one out)
# ALLOWED_ORIGINS_RAW=http://localhost,https://example.com
# Database
DATABASE_URL=postgresql+asyncpg://<username>:<password>@localhost:5432/vector_poc
# LLM
OPENAI_API_KEY=sk-xxx
LOCAL_LLM_PATH=/models/local-llm.bin
# HF
HF_TOKEN=<hf-token>
# Model flow like ollama or hf - inprogress
MODEL_FLOW=ollama

